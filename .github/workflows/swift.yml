# This workflow will build a Swift project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-swift

name: UnitTests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main", "release/*" ]

jobs:
  build:

    runs-on: macos-26

    steps:
    - uses: actions/checkout@v4

    - name: Select Xcode 16.1
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '16.1'

    # 1. Run Package Tests (Library Logic)
    - name: Run Package Tests
      run: swift test -v --enable-code-coverage

    # Debug: List available simulators to debug CI issues
    - name: List Available Simulators
      run: xcrun simctl list devices available

    # 2. Run Example App Tests (Integration via xcodebuild)
    # Using iPhone 16e on macos-26
    - name: Run Example App Tests
      run: |
        xcodebuild test \
          -project Example/HarborExample.xcodeproj \
          -scheme HarborExample \
          -destination 'platform=iOS Simulator,name=iPhone 16e' \
          -resultBundlePath TestResults \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_ALLOWED=NO \
          COMPILER_INDEX_STORE_ENABLE=NO
